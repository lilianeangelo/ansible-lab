[Unit]
Description=Simple Web Application
After=network.target

[Service]
User=root
WorkingDirectory={{ app_path }}
ExecStart=/usr/bin/python3 -m gunicorn --workers 3 --bind 0.0.0.0:8000 'app:app'
Restart=always

[Install]

WantedBy=multi-user.target
